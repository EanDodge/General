//class bigint and its methods
//cs23001
//bigint program
//Ean Dodge
const int capacity = 200;

class bigint{
public:
    bigint(){for(int i = 0; i < capacity; ++i)num[i]=0;}
    bigint(const char[]);
    bigint(int num);
private:
    int num[capacity];
};