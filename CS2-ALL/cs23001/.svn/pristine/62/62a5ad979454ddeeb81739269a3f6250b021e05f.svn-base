#include<iostream>
#include<cassert>
#include"string.hpp"

int main(){
    {
        String one;
        String two;
        String three;
        assert('\0' == one);
        assert('\0' == two);
        assert('\0' == three);
    }
}
